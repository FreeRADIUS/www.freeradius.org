'v2' logos have the modified monogram with different spacing between the components

non-editable text means the text has been converted to vectors
editable text means the text is actual text, but requires the correct fonts to be installed to display properly.

The fonts used were:
NeoSans Medium Italic
Tagline - Helvetica Neue 75 Bold
